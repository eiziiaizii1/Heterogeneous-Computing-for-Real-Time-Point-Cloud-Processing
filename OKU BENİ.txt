Build ve Compile için:

rm -rf build/*
cd build
cmake ..
make

-----------------------------------

Run'lamak için:  (VoxelSize'ı (son parametre) fazla yapınca az nokta çıkıyor) 


cd build
./voxel_downsample --input path/to/pointcloud.pcd --output downsampled.pcd --voxel-size 0.1 --threads 4